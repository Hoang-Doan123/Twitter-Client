<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Nền của tab -->
    <solid android:color="@android:color/transparent" />

    <!-- Đường viền của tab -->
    <stroke
        android:width="2dp"
        android:color="@color/light_green" /> <!-- Màu của đường viền -->
</shape>
